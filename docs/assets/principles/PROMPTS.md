# 教材插图 ImageGen 提示词

本文件记录 30 个实验插图经代码与技术语义复核后的最终可复现提示词；若现有成图已经正确表达、而初始提示词仍有含混之处，以这里的纠正版为准。

统一输出约束：ImageGen 栅格插图、横向 3:2、象牙白教材纸张、用户空间蓝色、内核对象琥珀色、成功路径绿色、边界或失败路径珊瑚色；最终缩放为 1200×800 PNG。

# ImageGen prompts for experiments 01–10

## 01 — Positioned and vectored file I/O

Hard accuracy lock: use zero-based byte boundaries. The backing file is exactly 11 bytes and no terminating NUL is stored or transferred. The shared-offset needle belongs to the amber open-file description: the logical `writev` phase gathers 5/1/5 bytes and advances it 0→11; a logical 5-byte `pwrite` overwrites `[6,11)` and an 11-byte `pread` reads `[0,11)`, while two `lseek` observation checkpoints confirm that neither positioned-I/O phase moves the needle from 11. The later `lseek` resets 11→0; completed `readv` scatters “hello” / space / “UNIX!” into 5/1/5-byte buffers and advances the needle 0→11. A short positive transfer or `EINTR` retries only the remainder by advancing the iovec cursor or explicit offset; premature EOF or zero before completion, or a hard error, is a coral failure. Depict each operation as a logical phase that may require multiple calls, never as an atomic one-call guarantee.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining positioned and vectored file I/O. Use a subtle ivory paper texture, crisp dark-charcoal linework, soft blue for user-space buffers and file descriptors, warm amber for the kernel open-file description and backing file, green for verified output, and coral only for semantic boundaries or failure cases. On the left, show three blue buffers containing “hello”, one space, and “world” flowing through one gathered write into an amber file. In the center, show a clearly separate shared-file-offset needle resting at byte 11. Show an explicit-offset write at byte 6 replacing “world” with “UNIX!” while bypassing and not moving that needle. Then show an explicit-offset read from byte 0 returning “hello UNIX!” while the shared offset still remains 11. Finally show the offset reset to zero and the file scattered back into three 5/1/5-byte buffers, ending in a green verification panel reading “hello UNIX!”. Use only these API labels, spelled exactly: `writev`, `pwrite`, `pread`, `lseek`, `readv`. Make the arrows distinguish implicit shared-offset I/O from explicit-offset I/O. Do not imply parallel I/O, do not imply that `pread` or `pwrite` changes the shared offset, and do not imply that a single call can never return a short transfer. Keep text minimal, with no paragraphs or code listings inside the image. Not Mermaid, not SVG, no watermark, no logos, no decorative computer screens.

## 02 — Directory-relative filesystem operations

Hard accuracy lock: the only file-size result is the exact prose “regular file · size 23 bytes”; never render a placeholder or example count such as 1234. Show the source-file content card with the exact payload `directory-relative I/O\n` and a visible final `\n`. The central directory descriptor is already open: do not invent or describe how it was obtained. The symlink inode stores the target name `source.txt`, but the `O_NOFOLLOW` probe must end at the final-component symlink with `ELOOP` and must not visually traverse into the regular-file inode.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining directory-relative filesystem operations. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for user-space path names and the trusted directory descriptor, amber for kernel directory and inode objects, green for successful metadata, rename, and cleanup results, and coral for the rejected symbolic-link path. Place a trusted directory file descriptor as the central anchor. From it, show relative creation of `source.txt` containing the exact 23-byte text "directory-relative I/O\n" (including the newline but excluding the terminating NUL), metadata inspection proving a regular file with size exactly 23 bytes, creation of `source.link`, and an attempted final-component open with a small `O_NOFOLLOW` flag that terminates in a coral `ELOOP` rejection. Continue the successful branch by renaming `source.txt` to `renamed.txt`, then removing the link and renamed file. Use only these API labels, spelled exactly: `openat`, `fstatat`, `symlinkat`, `renameat`, `unlinkat`. If any file-size count appears, it must be 23 bytes; never invent another value. Every filesystem-operation arrow must originate from the same directory-descriptor anchor rather than from a current-working-directory icon. Do not imply that `O_NOFOLLOW` protects intermediate components of a multi-component untrusted path, do not depict the whole path as race-proof, and do not imply that these operations depend on the process current working directory. Keep annotations short and visual, with no code blocks. Not Mermaid, not SVG, no watermark, no logos.

## 03 — Shared memory-mapped file synchronization

Hard accuracy lock: the actual run extends the file to exactly one page, maps exactly that same one-page extent from the same still-open fd at offset 0 as read/write `MAP_SHARED`, and performs `msync` with `MS_SYNC` across the entire page. After unmapping, show a short-read-safe `pread` loop that uses explicit offsets from zero and accumulates exactly 27 bytes, not a single-call guarantee and not a full-page read. Any `SIGBUS` panel must be explicitly hypothetical and separate from the actual run: only an additional whole mapped page wholly beyond current EOF is accessed. Do not apply `SIGBUS` to zero-filled bytes in a partial final page or to an address outside the VMA. `ftruncate` changes logical size and does not promise physical allocation.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining a shared writable file mapping. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for the user process and virtual mapping, amber for the file extent and kernel page cache, green for completed synchronization and successful readback, and coral for the out-of-range access boundary. Show an initially empty pathname-backed temporary file in an unspecified filesystem being extended to exactly one system page; never label or imply that the filesystem is tmpfs. Map that page into a blue user-space virtual page with a small `MAP_SHARED` badge. Show a normal memory write copying the exact 27-byte payload “visible through MAP_SHARED” (26 visible characters plus terminating NUL) into the mapping and immediately making the corresponding amber page-cache page dirty. Then show `msync` with `MS_SYNC` requesting synchronization of those dirty mapped-file updates and waiting for completion; do not say that `msync` is what first flushes or copies the data into the page cache, because the mapped store already dirtied that page-cache page. Next unmap, followed by a descriptor-based `pread` of exactly 27 bytes—not a full page—that returns the identical payload in a green verification box. Add one restrained coral dashed side case in which the virtual address is still inside an existing mapping but its corresponding offset lies beyond the current backing-file extent, leading to `SIGBUS`; do not depict this merely as an arbitrary address past the VMA boundary. Use only these API labels, spelled exactly: `ftruncate`, `mmap`, `msync`, `munmap`, `pread`. If any payload or read count appears, it must be 27 bytes. Do not portray `mmap` as copying the entire file into a separate buffer, do not portray `msync` as a guaranteed power-loss-safe physical-media commit, and show that the final read may still be served by the page cache. Keep labels sparse and avoid paragraphs. Not Mermaid, not SVG, no watermark, no logos.

## 04 — Linux open-file-description locking

Hard process/layer lock: show one process only. Blue cards are local fd-number handles; the kernel descriptor-table entries, OFD A, OFD B, inode, and lock table are separate amber objects. Two handles created by `dup` lead through separate descriptor-table entries to OFD A; the independently opened contender handle leads to OFD B. Use distinct nonblocking snapshots: OFD B makes a first fresh `fcntl` attempt and immediately gets `EAGAIN / EACCES`; closing only the original OFD-A handle leaves its duplicate alive, so OFD B makes a second fresh attempt and immediately gets the same conflict; the duplicate then explicitly unlocks, and only a third fresh attempt by OFD B succeeds. Never depict a queued waiter or automatic grant. Depict unlock as `fcntl` with command `F_OFD_SETLK` and `l_type = F_UNLCK`, not as a standalone `F_UNLCK` call. Closing the final reference to OFD A would also release the lock; this run instead demonstrates explicit unlock while the duplicate remains open. If any `struct flock` field is shown, `l_pid` must be zero.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining a Linux open-file-description byte-range lock. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for process-level file descriptors, amber for kernel open-file descriptions and the byte-range lock table, green for the eventual successful contender lock, and coral for lock conflicts. Clearly separate three layers: file descriptor table, open file descriptions, and one shared inode with byte interval `[0,1)`. Show an original descriptor and its duplicated descriptor both pointing to the same kernel object “OFD A”. Show an independently opened contender descriptor pointing to “OFD B”. OFD A acquires a write lock on `[0,1)` using the small command label `F_OFD_SETLK`; OFD B’s attempt reaches a coral conflict labeled `EAGAIN / EACCES`. Then show the original descriptor closing while the duplicate still points to OFD A and the lock remains. Finally show `F_UNLCK` through the duplicate, followed by OFD B successfully acquiring the interval in green. Use only these API labels, spelled exactly: `dup`, `open`, `fcntl`, `close`. Do not depict the lock as owned by the process or by one descriptor number, do not release it when only one duplicate closes, and do not imply mandatory enforcement against programs that ignore the locking protocol. Not Mermaid, not SVG, no watermark, no logos, no code listing.

## 05 — posix_spawn output capture through a pipe

Hard accuracy lock: depict only the Linux `pipe2(O_CLOEXEC)` fast path and never imply that `pipe2` is portable POSIX. The parent registers ordered file actions, but those actions modify only the spawned child's descriptor table before the external program starts, never the parent's table. The external `printf` sends exactly 20 bytes, `child: posix_spawnp\n`, including the visible final newline and excluding any terminating NUL, through a byte-stream pipe with no message boundary. The parent loops on `read`, appending short positive results and retrying `EINTR`; zero is EOF only after every writer reference is closed, including the child's standard output, so closing the parent's writer copy alone does not produce EOF. After EOF, `waitpid` confirms normal exit and status 0. Treat a nonzero `posix_spawnp` return as its returned error code rather than `errno`; once spawning succeeds, every later path must reap the child.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining parent-to-child descriptor setup and output capture with POSIX spawn. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for the parent and child program, amber for the kernel pipe and descriptor transformations, green for captured text and a zero exit status, and coral for a leaked-writer boundary. Show the parent creating a close-on-exec pipe with read and write ends. In a compact spawn-actions inset, show the child-side read end being closed, the pipe write end being duplicated onto standard output, and the redundant original write descriptor being closed only when it differs from standard output. Show the external executable named `printf` launched through runtime PATH search without inventing a resolved pathname such as `/usr/bin/printf`. If argv is shown, it must be exactly the four elements `"printf"`, `"%s"`, `"child: posix_spawnp\n"`, and null, so the external program prints the data through a format argument; never omit `"%s"`. Its standard output flows into the pipe. The parent closes its own write-end copy using a plain prose caption, not an extra `close` API label, reads until EOF, waits for the child, and verifies both “child: posix_spawnp” and exit status 0 in green. Add a coral side case: if the parent keeps a write end open, EOF cannot arrive. Use only these API labels, spelled exactly: `pipe2`, `posix_spawn_file_actions_adddup2`, `posix_spawn_file_actions_addclose`, `posix_spawnp`, `read`, `waitpid`. Do not render `close` as an additional API label, draw an explicit `fork` call, confuse the external `printf` executable with the C library function, or show the duplicated standard output being closed merely because the original pipe was created with close-on-exec. Keep text sparse. Not Mermaid, not SVG, no watermark, no logos.

## 06 — Reading a blocked signal through signalfd

Create a horizontal 3:2 premium editorial textbook raster illustration explaining synchronous signal consumption with Linux signalfd. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for process-owned controls and the signal descriptor, amber for kernel signal state, green for validated metadata, and coral for traditional delivery or cleanup hazards. Use four clearly separated regions and no numbered step circles. First, an upper setup strip shows: save original mask → `sigprocmask` blocks `SIGUSR1` → `signalfd` creates one blue signal descriptor for that blocked set. Second, a separate middle causal strip must show exactly five left-to-right objects with unobstructed arrowheads: `kill` → amber pending signal set containing `SIGUSR1` → blue signal descriptor marked readable → `read` → green metadata card containing `SIGUSR1` and the sending PID. Position `kill` immediately beside the pending-set box so its arrow cannot pass through the descriptor. The descriptor observes and consumes the pending signal; it never creates, forwards, or receives signal-delivery bytes. Label the pending standard signal compactly as “one pending instance; repeats may coalesce”. After metadata verification, show `close` and restoration of the original mask with `sigprocmask`. Third, a small coral side branch shows an unblocked signal bypassing the descriptor and following its traditional disposition: handler, default action, or ignore. Fourth, a compact cleanup strip shows the safe order for a sent but unread pending signal: consume it using `read` or `sigtimedwait`, then restore the original mask with `sigprocmask`; show restore-first only as a crossed-out hazard. Use only these API labels, spelled exactly: `sigprocmask`, `signalfd`, `kill`, `read`, `close`, `sigtimedwait`. Do not draw any arrow from the signal descriptor into the pending set, do not connect readable state to the blocked mask, do not depict `signalfd` as a signal handler or ordinary byte pipe, and do not imply that it captures unblocked signals automatically. Not Mermaid, not SVG, no watermark, no logos, no code listing, no text-heavy panels.

## 07 — Bounded producer-consumer queue with condition variables

Hard timeline/locking lock: the central full snapshot and final verification are different moments. The shown full snapshot `[9,10,7,8]`, `head=2`, `tail=2`, `count=4` is reachable after producing 10 and consuming 6, with intermediate sum 21; never attach the final `produced 20`, `consumed 20`, empty queue, and sum 210 to that snapshot. At count 4, tail index 2 is not a free slot; it becomes the next insertion position only after a removal creates space. Show at most one worker inside the queue critical section at a time, with queue and statistics updated under the same mutex. In each worker lane, `pthread_cond_signal` occurs while the mutex is still held and before `pthread_mutex_unlock`. Full/`not_full` and empty/`not_empty` waits are alternative states, never simultaneous; after `pthread_cond_wait` reacquires the mutex, recheck the predicate before modifying the queue. Main calls `pthread_create` once per worker, and `pthread_join` only waits for termination—it does not cause termination.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining a pthread producer-consumer queue. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for producer, consumer, and main threads as well as the user-space circular queue, mutex, and condition-variable objects; amber only for an optional compact kernel scheduling or futex-wait support layer; green for final counters and sum; and coral for the full and empty wait states. Keep the queue and pthread objects above the user-space/kernel-space boundary rather than presenting them as kernel-owned objects. Place the blue four-slot queue in the center with one mathematically self-consistent full snapshot: physical slots `[0]=9`, `[1]=10`, `[2]=7`, `[3]=8`; both head and tail equal index 2 and both arrows point to index 2; count equals 4; the logical removal order starting at head is 7, 8, 9, 10. Do not draw any other conflicting head or tail arrow. The producer inserts integers 1 through 20 only while holding the mutex; after insertion it signals `not_empty`. The consumer removes exactly 20 values only while holding the same mutex; after removal it signals `not_full`. Show a coral loop from “queue full” to a condition wait and back through a `while` predicate recheck, and a symmetric loop for “queue empty”. Explicitly illustrate that waiting atomically releases the mutex and reacquires it before return. Finish with the main thread joining both workers and a green result card: produced 20, consumed 20, queue empty, sum 210. Use only these API labels, spelled exactly: `pthread_create`, `pthread_mutex_lock`, `pthread_cond_wait`, `pthread_cond_signal`, `pthread_mutex_unlock`, `pthread_join`. Do not portray condition variables as stored events or message queues, show a sleeping waiter retaining the mutex, replace the predicate loop with a one-time check, or include C source-code loops/statements; use short conceptual labels rather than code blocks. Not Mermaid, not SVG, no watermark, no logos, minimal text.

## 08 — POSIX shared memory and a process-shared semaphore

Hard setup/causality lock: `ftruncate` sizes the object to exactly `sizeof(struct shared_state)` without inventing a numeric byte size, and `sem_init` initializes exactly one unnamed `sem_t` stored inside the shared mapping with `pshared=1` and initial value 0 before `fork`. After `mmap`, the shared-memory fd is closed before `shm_unlink` and `fork`; the child inherits only the mapping, whose references keep the successfully unlinked backing object alive. Do not impose scheduler order: the child's single `sem_wait` may block before the parent's single `sem_post`, or the post may happen first and leave one token for the later wait. The semaphore carries only that readiness token, never the `input` or `output` data, and there is no child-to-parent semaphore acknowledgement. Cleanup of the semaphore and parent's final mapping occurs only after `waitpid` confirms child termination. The PID-derived name uses exclusive creation; a collision fails rather than overwriting an object, so do not claim mathematical uniqueness.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining parent-child communication through POSIX shared memory. Use a subtle ivory paper texture, crisp dark-charcoal linework, blue for parent and child processes, their user-space virtual mappings, the shared fields, and the process-shared semaphore object; amber for the kernel POSIX shared-memory backing object and shared physical/page-cache backing; green for the computed result and successful child status; and coral for the namespace-lifetime boundary. Show creation of a PID-derived amber shared-memory backing object with exclusive-create semantics, sizing it to one shared structure, and mapping it into a blue user-space virtual region with a small `MAP_SHARED` badge. Show the name being unlinked in coral while the backing object and existing mapping remain alive through references. Inside the blue mapped region, show a process-shared semaphore plus fields `input 21` and `output 0`, initialized before the process split; do not place these directly accessed objects behind the kernel boundary. Then branch into parent and child after the input is written: the child inherits a blue mapping of the same amber backing pages and waits; the parent posts; the child computes and stores `output 42` and exits; the parent uses `waitpid` to reap and confirm successful child termination, and only then reads and verifies 42 in green. Do not show the parent calling `sem_wait` on the single posted semaphore. Use only these API labels, spelled exactly: `shm_open`, `ftruncate`, `mmap`, `sem_init`, `shm_unlink`, `fork`, `sem_wait`, `sem_post`, `waitpid`. Do not show the child reopening the object by name, do not destroy the live mapping when the name is unlinked, do not place the process-shared semaphore in ordinary private memory, and preserve the actual ordering in which input 21 is written before `fork` while the post happens afterward. Not Mermaid, not SVG, no watermark, no logos, no dense prose.

## 09 — Passing a file descriptor with SCM_RIGHTS

Hard layer lock: no blue parent/child table may contain a column, row, cell, or phrase named “open file description”. A blue process card may show only its local integer fd handle. Every such handle must cross the user/kernel boundary to a separate amber per-process descriptor-table entry, and only that amber entry may point to a separate amber open-file description. Do not color either the descriptor table or open-file description blue.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining Unix-domain file-descriptor passing. Use a subtle ivory paper texture and crisp dark-charcoal linework. Use blue only for parent and child user-space code, buffers, and integer fd-handle cards; use amber for the kernel-maintained descriptor tables, Unix-domain socket pair, pipe, and open-file descriptions; use green for successfully read payload and coral for malformed or truncated ancillary data. Make the boundary explicit: a blue fd number indexes an amber kernel descriptor-table entry, and that entry references an amber open-file description; never place an “open file description” column inside a blue user-space table. First show a datagram Unix socket pair being created and the process splitting. Only after that split, show the parent creating a pipe so the child cannot have inherited it. The parent uses `write` to place the exact 21-byte text `descriptor-passing-ok` into the pipe, excluding its terminating NUL. Separately, `sendmsg` carries exactly one ordinary data byte, the marker `'F'`, plus exactly one ancillary control message marked `SCM_RIGHTS` containing the pipe read descriptor. Visually distinguish the 1-byte ordinary-data arrow from the ancillary-control arrow; never call the 21-byte pipe content the sendmsg ordinary payload. Show the kernel installing a new descriptor-table entry and returning a new blue fd number to the child; both the parent-side source entry and child-side new entry reference the same amber kernel open-file description. The child reads exactly 21 bytes through the received fd and reaches a green payload verification state. Add a coral rejection branch for truncated, malformed, or multiple unexpected descriptors, with cleanup of every installed descriptor. Use only these API labels, spelled exactly: `socketpair`, `fork`, `pipe`, `write`, `sendmsg`, `recvmsg`, `read`. Do not depict file contents or the original descriptor number as the thing transferred, require the two descriptor numbers to match, or show the pipe as inherited across the process split. Not Mermaid, not SVG, no watermark, no logos, minimal text.

## 10 — Nonblocking loopback TCP connection completion

Hard execution lock: the server and client swimlanes are two roles inside one process on one host—never draw two processes, `fork`, two machines, or an external network. After either immediate `connect` success or successful `poll` plus zero `SO_ERROR`, show the client socket returning to blocking mode before `accept4` and the `ping`/`pong` exchange. The later transfer loops handle short positive transfers and `EINTR` only; do not show `EAGAIN`, nonblocking backpressure handling, or claim that one `send` or `recv` completes all four bytes.

Create a horizontal 3:2 premium editorial textbook raster illustration explaining a nonblocking IPv4 loopback TCP connection. Use a subtle ivory paper texture and crisp dark-charcoal linework. Use blue only for client/server user-space code, application buffers, and integer fd-handle cards; use amber for the kernel listener, client and accepted socket objects, TCP stack, readiness state, and dynamic port allocation; use green for a confirmed connection and successful exchange and coral for timeout or connection failure. Use two clean horizontal application swimlanes, server above and client below, connected only through an amber loopback kernel lane. The server code resolves numeric `127.0.0.1` with service 0, binds and listens through an amber socket object, then uses `getsockname` to observe the actual ephemeral port selected by the kernel; keep it symbolic as “actual port” and never invent a fixed port number. The client code resolves that actual port, receives a blue fd handle for an amber nonblocking socket object, and starts connecting. Draw two accurate branches: immediate connection success, or `EINPROGRESS` followed by readiness polling. On the readiness branch, show that writable readiness is followed by querying `SO_ERROR`; only error value zero reaches the green established-connection state, while a nonzero error or timeout goes coral. Show the server accepting a blue close-on-exec peer fd that references an amber connected socket object, then looped short-transfer-safe movement of exactly four application payload bytes client-to-server `ping` and exactly four bytes server-to-client `pong`, ending in green verification. These are application byte counts, not inherent TCP message boundaries. Use only these API labels, spelled exactly: `getaddrinfo`, `bind`, `getsockname`, `connect`, `poll`, `getsockopt`, `accept4`. Do not depict socket objects as user-space data structures, imply that `POLLOUT` alone proves success, omit the immediate-success branch, show any external network beyond loopback, use a fixed port number, or portray TCP as preserving application message boundaries or guaranteeing one-call full transfers. Not Mermaid, not SVG, no watermark, no logos, no paragraphs inside the image.

# ImageGen prompts for experiments 11–20

## 11 — epoll event loop with eventfd and timerfd

Hard result-layout lock: the `epoll_wait` result contains exactly readiness/source identities such as “eventfd ready” and “timerfd ready”, with no 7, 1, binary digits, counter cells, or payload arrows. From each readiness identity, draw a separate visible blue operation labeled `read` that crosses to its own amber source object; only after those two reads may the blue 64-bit values 7 and 1 appear. If two `read` labels cannot be shown, omit the values rather than implying that epoll returned them.

Create a premium editorial textbook raster illustration explaining a minimal Linux event loop, in a wide horizontal 3:2 composition on subtle ivory-white paper texture. Use blue for user-space actions and the event-loop controller, amber for kernel file-descriptor objects and readiness machinery, green for verified success, and coral only for timeout or error boundaries; use dark charcoal outlines and typography. On the left, show two independent inputs: a user-space wake value of 7 entering a kernel event counter, and a one-shot 20 ms monotonic timer entering a kernel expiration counter. In the center, show both readable file descriptors registered into one epoll interest set. Make the right-side sequence explicit and non-collapsible: first, `epoll_wait` returns only source-identity/readiness tokens, never 7, 1, or counter bits; readiness can name either source in either order. Second, in separate blue operations after readiness, `read` crosses the boundary once for the eventfd and once for the timerfd, consuming one 64-bit counter from each kernel object and only then producing blue values 7 and 1. End at a green check marked “7 and 1 verified.” Include exactly these API labels, spelled precisely: `eventfd`, `timerfd_create`, `timerfd_settime`, `epoll_create1`, `epoll_ctl`, `epoll_wait`, `read`. Keep all other wording extremely sparse. Make the user-space/kernel boundary visually explicit. Do not put any counter digits or payload bits inside the `epoll_wait` result, imply that epoll carries the counter payload, make event order deterministic, make the timer periodic, or portray eventfd and timerfd as signals or threads. Scientific-educational, clean systems-programming cutaway, not Mermaid, not SVG, no code listing, no watermark.

## 12 — inotify filesystem event stream

Create a premium editorial textbook raster illustration of one Linux inotify filesystem-monitoring run, wide horizontal 3:2 on lightly textured ivory paper. Use blue only for user-space path strings, action requests, the aligned read buffer, and parser; use amber for the actual watched directory, file/VFS objects, inotify watch, and kernel event queue; green for the accumulated required event set; and coral for overflow, malformed records, timeout, EOF, or cleanup failures. Begin with a blue path card labeled exactly “template: ./lab-inotify-XXXXXX”; the directory is created empty and the six X characters are replaced at runtime. Never render `/tmp`, `/var/tmp`, `/data`, a concrete suffix, or any absolute path. Use this strict normal-path order: empty runtime directory → `inotify_init1` with `IN_CLOEXEC | IN_NONBLOCK` → `inotify_add_watch` on that directory with `IN_CREATE | IN_MODIFY | IN_CLOSE_WRITE | IN_DELETE` → create `sample.txt` → write and close it → `unlink` it → begin the `poll` / `read` loop. Render create, write, and close as plain action captions, not additional API labels. By the first poll, the file is already absent and the queued notifications are historical; if content appears, it must be exactly the 8-byte payload `inotify\n`, including the final newline and excluding a terminating NUL. Make the user/kernel boundary explicit. `poll` reports readiness only and never transfers records. Only `read` crosses the boundary into a distinct aligned blue object prominently labeled “4096-byte user buffer”; one read may copy one or more complete variable-length records. In every record, `len` is a field inside the fixed `struct inotify_event` header, not a separate segment; total record size is `sizeof(struct inotify_event) + event->len`, and `event->len` sizes the variable `name[]` storage including NUL and padding. The parser checks `IN_Q_OVERFLOW` before any wd/name filter; overflow is immediate coral failure and never rejoins green success. Only records with the watched wd, nonzero name length, and exact name `sample.txt` contribute bits. Accumulate `IN_CREATE`, `IN_CLOSE_WRITE`, and `IN_DELETE` across any number of records and read cycles; `IN_MODIFY` may appear but is not required, and no fixed record order or one-record-per-action mapping is promised. Retry `EINTR`, return `EAGAIN` to poll, and route a 1000 ms timeout, error readiness, zero-length read, malformed record, truncated record, or trailing partial record to coral failure. Place `inotify_rm_watch` only after the green accumulated set, followed by plain-prose closing of the notification fd and removal of the now-empty directory. Include exactly these API labels, spelled precisely: `inotify_init1`, `inotify_add_watch`, `poll`, `read`, `unlink`, `inotify_rm_watch`. Do not depict inotify as transactional, recursive, overflow-free, or a permanent audit log. Not Mermaid, not SVG, no source-code panel, no watermark.

## 13 — splice file-to-pipe transfer

Treat the top-row setup objects, crossed-out staging buffer, and retained source payload as independent context callouts: never connect them with directional arrows or present them as a data path or execution sequence. The only payload-transfer arrow must be the kernel-space `splice` path from the opened file/page cache into the pipe buffer.

Create a premium editorial textbook raster illustration of Linux file-to-pipe transfer with splice, wide horizontal 3:2 on an ivory paper texture. Use blue for user-space setup and final verification, amber for the kernel page cache and pipe buffer, green for matching length and content, and coral for short-transfer, interruption, and capacity boundaries. Use three clearly labeled chronological horizontal swimlanes: upper USER SPACE for setup and the crossed-out staging buffer, middle KERNEL SPACE for the amber page cache, splice path, pipe buffer, and capacity note, and lower USER SPACE — VERIFICATION for the blue read, user buffer, memcmp, and match result. Separate adjacent lanes with crisp boundaries; never place a blue application or verification object in the KERNEL SPACE lane, and never place an amber kernel object in either USER SPACE lane. Show an opened and unlinked temporary file containing exactly the literal payload `splice: regular file -> pipe, without a user-space copy\n`; render the final newline as the visible escape `\n`, do not substitute any other sentence, and label this payload as 56 bytes excluding its terminating NUL. Draw the main transfer arrow directly from the file/page-cache side into the pipe buffer through `splice`, visibly bypassing only the crossed-out user-space staging buffer. Any explanatory caption about this bypass must state exactly “splice uses no user-space transfer buffer”; never claim that no payload data exists or is ever staged in user space, because the program first writes the source file and later reads for verification. Beside that arrow, show a concise explicit-offset cursor named `input_offset`: it starts at 0, advances by each successful short transfer, and ends at 56. Never render a null input-offset argument and do not imply that the descriptor's shared file offset is used or changed. Show a small conceptual loop motif only for partial progress and `EINTR`. Separately annotate that pipe capacity is finite and a larger blocking transfer may wait for a reader, while this empty-pipe 56-byte demonstration does not trigger that boundary; do not claim the application loop handles `EAGAIN` or actively drains backpressure. After all 56 bytes have moved, show a plain prose step “pipe write end closed” without adding another API label; only below that sequence, show a separate blue verification step using `read` to collect exactly 56 bytes from the pipe into a user buffer and `memcmp` to compare them with the exact original payload. Include exactly these API labels, spelled precisely: `mkstemp`, `unlink`, `pipe2`, `splice`, `read`, `memcmp`. If any transfer or payload count appears, it must be 56 bytes; show no other payload text or byte count. The illustration must be purely conceptual: render no C declarations, function-call signatures, pathnames, braces, semicolons, comments, flags, pseudocode, or code panels; use only sparse noun-and-action captions. Do not claim that the entire program performs zero copying, that no user-space read occurs during verification, that `splice` works between arbitrary descriptor types, or that a pipe has unlimited capacity. Clean kernel-data-path cutaway, not Mermaid, not SVG, no benchmark imagery, no watermark.

## 14 — reversible resource-limit change

Create a premium editorial textbook raster illustration of one Linux process safely inspecting and temporarily replacing `RLIMIT_NOFILE`, in a wide horizontal 3:2 layout on subtle ivory paper. Use blue for the process and runtime-dependent snapshots, amber for the kernel resource-limit pair, green only after verified restoration and final success, and coral for the active guarded-restore path. Render exactly two initial query call cards: first `getrlimit`; second `prlimit` labeled “query only — no new limit”. Both observe the same current-process pair `(S0, H0)`, and the second card must not contain `getrlimit`. All `prlimit` operations target pid 0. Compute `T = min(S0, 64)`, where 64 is the only fixed numeric limit value. Then a temporary `prlimit` accepts the complete pair `(T, H0)` and atomically returns the previous complete pair `(S0, H0)` into a distinct “returned old pair” snapshot; immediately compare that returned pair with the original. Even when `S0 <= 64` and therefore `T = S0`, still perform this replacement, returned-old comparison, the next verification, and restoration—never bypass them as a no-op branch. Next, one `getrlimit` confirms the active pair `(T, H0)`. Label `H0` “preserved by this experiment”; never use a padlock or claim hard limits are intrinsically immutable. Restore with a final `prlimit` that accepts the exact original pair `(S0, H0)` and requests no previous-value output. Do not add a post-restore `getrlimit`. Only after restoration succeeds, show `getrusage` for `RUSAGE_SELF` producing symbolic runtime-dependent user and system CPU-time snapshots, never a percentage or limit value. The coral restore guard activates only after the temporary replacement succeeds and remains active only until restoration succeeds; failures before replacement or after successful restoration need no restore, while a failed first restoration stays coral and routes to a cleanup restoration attempt. Include exactly these API labels, spelled precisely: `getrlimit`, `prlimit`, `getrusage`. Do not show deliberate descriptor exhaustion, privileged increases, or getrusage as a limit-setting operation. Premium scientific systems diagram, not Mermaid, not SVG, minimal text, no watermark.

## 15 — CPU affinity within a container cpuset

Hard runtime-state lock: depict exactly one process with one calling thread; PID 0 affects only that calling thread, never every thread. Treat the effective allowed mask, its count, and the selected CPU identifier as runtime-dependent symbolic values—never invent a fixed CPU count or number. The set may be non-contiguous but need not be. After the singleton mask has been installed, every failure path attempts to restore the saved original mask; green success requires both singleton verification and a final exact `CPU_EQUAL` restoration check. Keep user-space mask values/cards blue and scheduler/cgroup eligibility state amber or coral.

Create a premium editorial textbook raster illustration of temporary CPU-affinity pinning, wide horizontal 3:2 on lightly textured ivory-white paper. Use blue for the calling thread and CPU-mask construction, amber for the Linux scheduler and allowed CPU set, green for verification and restoration, and coral for the container/cgroup boundary and invalid assumptions. Begin with an amber row of symbolic CPU cores and a coral-outlined container cpuset showing one clearly labeled example allowed subset that may be non-contiguous; do not present that example as a universal topology. Show the calling thread reading and saving that original mask, using `CPU_COUNT` to confirm it is nonempty, selecting the first CPU that is actually present rather than assuming CPU 0, constructing a singleton mask, asking the scheduler to bind the calling thread, rereading and using `CPU_COUNT` plus `CPU_ISSET` to verify exactly one allowed CPU, and finally restoring and comparing the complete original mask. Include exactly these API labels, spelled precisely: `sched_getaffinity`, `CPU_ZERO`, `CPU_COUNT`, `CPU_ISSET`, `CPU_SET`, `sched_setaffinity`, `CPU_EQUAL`. Do not imply that CPU identifiers are contiguous, that CPU 0 is always available, that PID 0 changes every thread in the process, that affinity bypasses cgroup restrictions, or that setting affinity guarantees immediate migration. Editorial scientific illustration, not Mermaid, not SVG, no dashboard styling, no watermark.

## 16 — PTY master, slave, and raw termios

Create a premium editorial textbook raster illustration explaining a pseudoterminal pair and termios, wide horizontal 3:2 on subtle ivory paper. Use blue for the user-space program and the PTY master/slave descriptors, amber for the kernel PTY driver and terminal line discipline, green for byte-for-byte verification and restored settings, and coral for disabled canonical processing and cleanup risk. Show the exact setup order: create a PTY master, authorize and unlock its slave, discover the slave device name, then use `open` with concise `O_RDWR | O_NOCTTY` annotations to obtain the slave descriptor; only after that open may the program use `tcgetattr` to save the slave's original terminal attributes and `tcsetattr` to apply a raw-style configuration to that slave. In the center, place the amber PTY driver between master and slave descriptors, with two distinct arrows: master writes the exact 16-byte string `master-to-slave\n` → slave reads those 16 bytes, and slave writes the exact 16-byte string `slave-to-master\n` → master reads those 16 bytes; both lengths exclude terminating NULs. On the right, show both received byte strings matching and the original termios being restored on the slave. Include exactly these API labels, spelled precisely: `posix_openpt`, `grantpt`, `unlockpt`, `ptsname`, `open`, `tcgetattr`, `tcsetattr`. If any transfer count appears, it must be 16 bytes in each direction. A small coral annotation may show canonical mode, echo, signal-character handling, and output processing disabled. Do not depict the PTY as two ordinary pipes, skip opening the slave descriptor, apply raw mode to the master, invent a shell or child process, or imply acquisition of a controlling terminal. Not Mermaid, not SVG, no terminal screenshot, no watermark.

## 17 — pidfd-based child-process waiting

Create a premium editorial textbook raster illustration of waiting for a Linux child process through a pidfd, wide horizontal 3:2 on lightly textured ivory paper. Use blue for parent and child user-space processes, amber for the kernel process object, pidfd, poll readiness, and wait machinery, green for validated exit and completed reaping, and coral for timeout, unsupported-kernel, or fallback cleanup. Show a parent forking a child; the child exits with status 42. The parent obtains a stable pidfd referring to that exact child, and the child’s exit changes the pidfd to readable. Show the parent waiting for readiness and then obtaining a siginfo-style result with PID, `CLD_EXITED`, and status 42, ending in a green “reaped” state. Add a small coral dashed fallback from error cleanup to conventional child reaping, and a crossed-out circular “bare PID reuse race” beside the stable pidfd. Include exactly these API labels, spelled precisely: `fork`, `pidfd_open`, `poll`, `waitid`, `waitpid`; place `P_PIDFD` as a small argument annotation beside `waitid`, not as another API. Do not show reading exit status from the pidfd, claim that poll itself reaps the child, place waitpid on the normal success path, or portray pidfd as portable to all Unix systems. Not Mermaid, not SVG, minimal labels, no watermark.

## 18 — POSIX message-queue priority ordering

Create a premium editorial textbook raster illustration of a POSIX message queue delivering by priority, wide horizontal 3:2 on subtle ivory-white paper. Use blue for the sending and receiving program, amber for the kernel message-queue object, green for verified priority order and cleanup, and coral for namespace/resource-limit boundaries or cleanup hazards. Show creation of one uniquely named queue with exclusive creation, configured with maximum message count 4 and message size 64. From the left, show the exact 13-byte message `low priority\0` (12 visible characters plus NUL), explicitly labeled “priority 1,” sent first; then show the exact 14-byte message `high priority\0` (13 visible characters plus NUL), explicitly labeled “priority 7,” sent second into the same amber queue. Those 1 and 7 numbers are priorities, never message text or lengths. Inside the queue, visibly arrange delivery by priority, then draw the priority-7 14-byte message leaving first and the priority-1 13-byte message second. Show a compact attribute check for current queued messages 2 and message size 64; maximum count 4 may also be shown as the configured value. Then verify each received content, exact length, and priority, close, and remove the queue name. Include exactly these API labels, spelled precisely: `mq_open`, `mq_send`, `mq_getattr`, `mq_receive`, `mq_close`, `mq_unlink`. Do not depict cross-priority delivery as simple FIFO, treat the leading-slash queue name as an ordinary filesystem pathname, claim that `/dev/mqueue` must be mounted for mq_open, or imply that unlink instantly destroys references that remain open. Not Mermaid, not SVG, no UI widgets, no watermark.

## 19 — file creation permissions and timestamps

Create a premium editorial textbook raster illustration of safe file-metadata manipulation, wide horizontal 3:2 on lightly textured ivory paper. Use blue for the process, private directory, and file descriptor, amber for VFS permission and timestamp operations, green for restored process state and verified final metadata, and coral for the process-wide umask hazard, default-ACL variation, and cleanup paths. Show a private mode-0700 temporary directory and an open directory descriptor. The process saves its original umask, temporarily sets 0027, and requests mode 0666 while creating `sample` relative to the directory descriptor. At the user/kernel boundary, split the Linux creation-permission logic into alternatives: without a parent default ACL, mode is filtered by the umask; with a default ACL, that ACL is inherited and constrained by the requested mode instead of first mechanically applying umask and then layering the ACL. Both alternatives lead to an observed initial mode rather than a guaranteed 0640. Show the original process-wide umask restored immediately and rechecked. Then the still-open file descriptor receives exact mode 0600 and two exact timestamps: atime 1700000000.123456789 and mtime 1700000123.987654321. A final `fstat` verifies mode and both second/nanosecond fields before cleanup. Include exactly these API labels, spelled precisely: `mkdtemp`, `umask`, `openat`, `fstat`, `fchmod`, `futimens`. Every error path while the temporary umask is active must visibly return to restoration. Do not imply that umask is thread-local, that it modifies an existing file, that initial mode must always equal 0640, that fchmod creates the file, or that every filesystem preserves nanosecond timestamps. Not Mermaid, not SVG, no code block, no watermark.

## 20 — sparse file and data/hole queries

Create a premium editorial textbook raster illustration of a Linux sparse file, wide horizontal 3:2 on subtle ivory paper. Use blue for user-space positioned writes and observations, amber for the VFS logical file and allocated storage, green for successful size/extent validation, and coral for unsupported or legally coarse extent-query behavior. Show one small `HEAD` data island written at offset 0 and one small `TAIL` data island written at offset 8 MiB, with a very long visually empty logical hole between them. Under the logical file, show only a few allocated physical blocks, making logical size approximately 8 MiB while allocated bytes are much smaller. Then show two analysis branches: metadata comparing logical size with `st_blocks × 512`, and data/hole queries locating the next data and hole boundaries. The query branch must split into a green detailed-layout validation and a coral-bordered but nonfatal “unsupported or coarse answer — extent check skipped” result. Include exactly these API labels, spelled precisely: `mkstemp`, `pwrite`, `fstat`, `lseek`, `unlink`; place `SEEK_DATA` and `SEEK_HOLE` as argument annotations beside `lseek`, not as separate APIs. Do not depict the hole as physically allocated zero-filled blocks, call `st_blocks` the filesystem block size, require extent boundaries to match the four written bytes exactly, claim that SEEK_DATA/SEEK_HOLE reveal exact physical extents, or present a legal coarse answer as experiment failure. Premium storage-system cutaway, not Mermaid, not SVG, no watermark.

# ImageGen prompts for experiments 21–30

## 21 — stdio user-space buffering

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for user-space streams, buffers, and file descriptors; amber for the kernel pipe and transfer boundary; green for verified output; coral for implementation-dependent or error boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Explain the actual left-to-right data flow of this experiment. Show a blue user-space writer where `pipe` first creates a read end and write end, `fdopen` wraps the write descriptor as a FILE stream, and `setvbuf` installs a 128-byte full buffer before any stream I/O. Show `fwrite` placing the exact 16-byte payload "buffered payload" (8 letters, one space, 7 letters; excluding the terminating NUL) into that user-space buffer. From the FILE buffer to the amber kernel pipe, draw two visually distinct routes: a thin dashed arrow labeled only "implementation-dependent early transfer" and a strong solid arrow through `fflush`. At the blue nonblocking reader, show an initial probe that may see zero, partial, or complete data, followed by reading any remainder after the flush. Merge both observations into one green exact-payload verification. Add a small ownership cue that the FILE stream owns the wrapped write descriptor.

Render exactly these six API labels, with precise spelling: `pipe`, `fdopen`, `setvbuf`, `fwrite`, `fflush`, `read`. If any byte count appears, it must be 16 bytes; never show 15. Do not render any other API or function names. Do not imply that the pre-flush count must always be zero, that `fflush` provides disk durability, that this pipe writes to storage, or that the wrapped descriptor should be closed independently from its FILE stream.

## 22 — process groups and sessions

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for user-space processes and pipe endpoints; amber for kernel session and process-group state; green for validated relationships and successful child reaping; coral for preconditions, short-I/O, and failure boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Depict the real execution order from left to right. A blue parent first creates a report pipe, then splits into parent and child. Make the endpoint ownership unmistakable after the split: the child closes only its unused read end, keeps the write end, and writes the report; the parent closes only its unused write end, keeps the read end, and reads the report. Never label the parent as the report writer or the child as closing the write end. The child invokes `setsid`; the amber kernel creates a new session and a new process group. The child queries the resulting identities and produces a green symbolic equality badge: "child PID = child PGID = child SID"; do not invent concrete numeric IDs. It sends one fixed-size PID/PGID/SID/error report through the pipe's write end. The parent receives the complete report through the read end with a small coral reminder for short transfers and EINTR retry, and then uses `waitpid` to reap the child. It jointly verifies the child's successful exit and the reported PID/PGID/SID relationships. End with a green "validated and reaped" state.

Render exactly these six API labels, with precise spelling: `pipe`, `fork`, `setsid`, `getpgid`, `getsid`, `waitpid`. Do not render any other API or function names. Show a coral boundary stating concisely that a process-group leader cannot successfully create a session this way. Do not depict a controlling terminal being created, the parent joining the child's new session, the parent writing the report, the child closing its report write end before sending, the parent reaping before receiving the report, report validation as complete before `waitpid` and exit-status verification, concrete made-up PID/PGID/SID numbers, or `setsid` as merely renaming an existing group.

## 23 — race-free portable signal waiting

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for parent and child user-space activity; amber for signal masks, pending state, and atomic kernel delivery; green for the signal-safe flag and verified completion; coral for race windows and error boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Show one strictly numbered top-to-bottom timeline whose vertical positions cannot be misread: 1) the blue parent uses `sigprocmask` to block SIGUSR1; 2) it installs the handler with `sigaction`; 3) only then it creates the child with `fork`; 4) the child uses `kill` to send SIGUSR1 to the parent and exits, while the blocked signal becomes one amber pending token and the handler flag stays zero; 5) the parent completely reaps the sender, with a plain caption "sender reaped" and no additional API label; 6) only below that reaping step, the parent uses `sigpending` to verify the token is pending and the handler flag is still zero; never place `sigpending` above or before sender reaping; 7) `sigsuspend` acts as one indivisible amber gate that temporarily installs a mask with SIGUSR1 unblocked and sleeps atomically, the pending signal passes through, the handler performs only a green sig_atomic_t flag update, and `sigsuspend` normally returns -1 with EINTR while automatically restoring the pre-suspend blocked mask; 8) afterward, explicit cleanup uses `sigprocmask` to restore the process's original mask and `sigaction` to restore the original action.

Render exactly these six distinct API labels, with precise spelling: `sigprocmask`, `sigaction`, `fork`, `kill`, `sigpending`, `sigsuspend`; `sigprocmask` and `sigaction` may each repeat at explicit cleanup. Do not render any other API or function names. Make the avoided "check-then-sleep lost wakeup window" visible as a crossed-out coral gap. Do not show ordinary SIGUSR1 instances forming a counting queue, do not show formatted output or allocation inside the handler, do not imply that `sigsuspend` permanently changes the mask, do not depict its normal wakeup as a zero return, and do not imply that returning from the signal handler automatically restores the old signal action.

## 24 — POSIX thread-specific data

Hard spatial/lifetime lock: place the shared `pthread_once` gate immediately to the right of the three worker labels and strictly to the left of every allocation, value icon, id payload, and per-thread TSD slot. All three worker arrows enter this gate before any value-related shape appears; no allocation or value arrow may appear anywhere to its left. This illustration requires no kernel object: render zero amber shapes, no amber legend entry, and no kernel-space region. Keep the once-control, process-wide key, per-thread TSD slots, private values, destructor callback, and relaxed atomic counters entirely blue or green in user space. The initializer winner is unspecified; other `pthread_once` calls wait if necessary and return only after the one initialization routine finishes. A worker allocates only if `pthread_once` succeeded and the single `pthread_key_create` recorded success; key-creation failure produces no allocation, no TSD value, no destructor, and no retry of once initialization. Depict the shared key only as an index/handle; each worker owns a separate slot indexed by that key. `pthread_setspecific` stores the allocation pointer in the current worker's slot, and `pthread_getspecific` returns that identical pointer and id—never copied content or a value stored inside the key. One common destructor is registered by the single key creation. Ownership transfers to the TSD exit path only after a successful set; a failed set releases the allocation immediately and causes no later destructor. Draw each normal worker return flowing into exactly one destructor callback and then a terminated state; only then may the corresponding `pthread_join` return. Never draw join causing a destructor. Branch join failure directly from `pthread_join`, before assuming that worker terminated or its destructor ran; totals may then be incomplete and the key is retained. Destructors may run concurrently in any order; relaxed atomic updates count them but do not serialize them, and main reads totals only after all created-worker joins succeed. Treat “destructors = 3, id sum = 6” as a pre-deletion check. Final verification additionally requires three created workers, all pointer/id checks, successful key initialization, and successful `pthread_key_delete`; key deletion invokes no destructor.

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for worker threads, their private values, the process-wide pthread key and runtime transitions, and the once-control object; green for successful per-thread verification and destructor totals; coral for lifetime and join-failure boundaries. Use no amber: this diagram contains no kernel object and no kernel-space lane. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Depict three parallel blue worker lanes labeled thread 1, thread 2, and thread 3. In every lane, the very first worker action is a call to `pthread_once`; all three calls converge on one blue user-space once-control object, but only one selected call invokes `pthread_key_create`, producing a single blue process-wide key. Keep that key and the once-control above the user-space/kernel-space boundary: pthread TSD and the once-control are user-space library/runtime abstractions, not kernel objects. Only after `pthread_once` has returned successfully may each worker allocate its distinct blue value containing its own id. Make the per-lane order visually strict and left-to-right: `pthread_once` success → allocate private value → `pthread_setspecific` with the one shared key → `pthread_getspecific` recovers exactly the same private value → thread exits → registered destructor releases that allocation and adds its id to relaxed atomic counters. Never draw an allocation before `pthread_once`. The main thread uses `pthread_join` for all created workers, then verifies the green result "destructors = 3, id sum = 6", and only after successful joins invokes `pthread_key_delete`. Include a coral side branch: if a join fails and a thread may still run, retain the key rather than deleting it.

Render exactly these six distinct API labels, with precise spelling: `pthread_once`, `pthread_key_create`, `pthread_setspecific`, `pthread_getspecific`, `pthread_join`, `pthread_key_delete`; repeated `pthread_once`, set/get, and join labels are allowed in their lanes. Do not render any other API or function names. Do not draw one key per thread, do not place allocation before successful `pthread_once`, do not suggest that `pthread_join` triggers the destructors, do not suggest that key deletion invokes destructors for live threads, and do not depict one worker reading another worker's value.

## 25 — named FIFO with nonblocking endpoints

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for parent, child, and file descriptors; amber for the filesystem FIFO node and kernel byte stream; green for the exact delivered payload and cleanup; coral for timeout, hangup, and blocking boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Show the actual ordered workflow. First create an amber named FIFO node inside a private temporary directory with `mkfifo`. The blue parent uses `open` with a concise O_NONBLOCK annotation on the read end before the parent/child split, so it does not wait for a writer. In the child lane, close the inherited read end, use the same `open` label for a nonblocking write endpoint, perform one `write` of the exact 27-byte message (26 visible characters plus its terminating NUL), which is smaller than PIPE_BUF, and exit. In the parent lane, show `waitpid` confirming that the nonblocking writer completed successfully before `poll` begins its bounded wait. Then show repeated `read` transfers from the amber FIFO byte stream until the known 27-byte length is complete, followed by a green payload match and removal of the FIFO node and temporary directory.

Render exactly these six distinct API labels, with precise spelling: `mkfifo`, `open`, `write`, `waitpid`, `poll`, `read`. The `open` label may appear at both endpoint cards, but do not render any other API or function names. If any payload byte count appears, it must be 27. Mark timeout, invalid descriptor, error, and premature EOF as coral exits. Do not depict `poll` moving the bytes itself, do not draw inherent message boundaries in the FIFO, do not claim that writes larger than PIPE_BUF are atomic, and do not show the filesystem FIFO disappearing automatically when the processes exit.

## 26 — System V message queue

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for parent and child processes; amber for the persistent kernel message queue and its attributes; green for the selected payload and explicit removal; coral for capacity, type, and cleanup boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Depict `msgget` with a concise IPC_PRIVATE annotation creating one new amber kernel queue and returning a queue ID. Through `fork`, show the ID inherited by a blue child and parent. The child builds one message with a positive long type 7 and a separate 36-byte text body (35 visible characters plus its terminating NUL), then uses `msgsnd` with an IPC_NOWAIT annotation and exits. The parent uses `waitpid` to confirm successful sending, then `msgctl` with IPC_STAT to show queue count 1. Next, `msgrcv` selects type 7 nonblockingly, removes the message, and sends its verified text to a green result card. A second IPC_STAT observation shows queue count 0. End every path at `msgctl` with IPC_RMID, visibly deleting the amber kernel object.

Render exactly these six distinct API labels, with precise spelling: `msgget`, `fork`, `msgsnd`, `waitpid`, `msgrcv`, `msgctl`. The `msgctl` label may appear for both observation and removal, but do not render any other API or function names. If any body-size count appears, it must be 36 bytes. Include a compact coral boundary showing that the positive long type field is not counted in the message-body size. Do not portray IPC_PRIVATE as an access-control guarantee, do not draw this queue as an untyped byte stream, do not leave the received message in the queue, and do not imply that the kernel queue vanishes when the final process exits.

## 27 — POSIX asynchronous file read

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for the caller, open file descriptor, aiocb, buffer, implementation-neutral AIO control state, and a possible user-space worker; amber only for the kernel open-file object and actual I/O boundary; green for the verified bytes and correctly reaped request; coral for in-progress, cancellation, and lifetime boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Show an already open and unlinked temporary file containing the fixed 28-byte payload (excluding its terminating NUL), next to a blue file descriptor, read buffer, and aiocb tied together by one lifetime bracket. The caller submits an offset-zero request with `aio_read` and aio_nbytes equal to 28. In a blue implementation-neutral completion state machine above the kernel boundary, `aio_error` either reports EINPROGRESS or a final status. Do not place the entire POSIX AIO implementation below the kernel boundary: for ordinary files it may use user-space worker threads. The EINPROGRESS path goes through `aio_suspend` and loops back to recheck status, making clear that wakeup alone is not completion. On successful final status, call `aio_return` exactly once, transfer the resulting byte count to a green content-match card, and then `close` the descriptor. Add a coral cleanup branch for any later failure while the request remains outstanding: use `aio_cancel` only as an attempt, still wait until the request leaves EINPROGRESS, invoke the same one-time `aio_return` recovery, and only then release the resources.

Render exactly these six API labels, with precise spelling: `aio_read`, `aio_error`, `aio_suspend`, `aio_return`, `aio_cancel`, `close`. Do not render any other API or function names. If any payload or returned-byte count appears, it must be 28. State visually, with minimal wording, that fd, buffer, and aiocb remain valid until the request is reaped. Do not depict `aio_suspend` as guaranteeing success, do not depict `aio_cancel` as guaranteed cancellation, do not call this io_uring or Linux native AIO, and do not insist on a kernel-only implementation because POSIX AIO for ordinary files may be implemented with user-space worker threads.

## 28 — file extended attributes through a descriptor

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for the already-open file descriptor and caller buffers; amber for the filesystem's separate extended-attribute metadata; green for matched value and list verification; coral for ENODATA, ERANGE, and unsupported-filesystem boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Depict one blue descriptor pointing stably to a temporary file while all operations address the amber metadata namespace rather than the file's content bytes. `fsetxattr` with a concise XATTR_CREATE annotation creates the name user.advance_unix and the exact 19-byte value "descriptor metadata" (excluding its terminating NUL). Show `fgetxattr` as a two-step size query followed by value retrieval into a blue buffer, ending in a green exact 19-byte length-and-value check. In a second branch, show `flistxattr` first querying the runtime total list size without asserting a fixed total, then returning a compact sequence of NUL-separated names that contains the exact 18-byte record `user.advance_unix\0` (17 name bytes plus one NUL); other accessible attribute names could make the total list larger. Merge the successful branches, then use `fremovexattr`. To prove removal, show exactly one final direct `fgetxattr` call with a non-null value buffer; it immediately returns coral ENODATA and the path stops. Do not draw a final size-query/retrieve pair or continue to a retrieve after ENODATA.

Render exactly these four API labels, with precise spelling: `fsetxattr`, `fgetxattr`, `flistxattr`, `fremovexattr`. Repeated appearances of the same label are allowed, but do not render any other API or function names. Any value-size count must be 19 bytes; any count attached specifically to the target name record including its separator must be 18 bytes; do not claim the entire name list is always exactly 18. Include only tiny boundary notes for concurrent-size change causing ERANGE and for filesystem, mount, or security policy denying user attributes. Do not draw the attribute inside the regular file contents, do not represent it as a sidecar file, do not render the name list as one ordinary string, and do not imply universal filesystem support.

## 29 — memfd file seals

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for the descriptor, initial data, and permitted reader; amber for the anonymous kernel memfd and its one-way seal state; green for verified seals and successful reading; coral for EPERM, EBUSY, and forbidden mutation boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Show `memfd_create` with concise MFD_ALLOW_SEALING and close-on-exec annotations producing one anonymous amber memory-backed file descriptor with no pathname. Load the exact 21-byte initial payload "sealed in-memory file" (excluding its terminating NUL) before sealing. Then show two ordered `fcntl` stages: first F_ADD_SEALS with F_SEAL_WRITE, F_SEAL_GROW, and F_SEAL_SHRINK together; second and last, F_ADD_SEALS with F_SEAL_SEAL. A following `fcntl` F_GET_SEALS checkpoint turns green after all four bits are observed. Split the sealed object into three tests: `pwrite` at offset zero reaches coral EPERM; `ftruncate` on both a larger and smaller size reaches coral EPERM; `pread` still returns the unchanged payload to a green reader. Also show any later seal-set update reaching EPERM because F_SEAL_SEAL is present.

Render exactly these five distinct API labels, with precise spelling: `memfd_create`, `fcntl`, `pwrite`, `ftruncate`, `pread`. The `fcntl` label may repeat at the seal stages, but do not render any other API or function names. If any payload-size count appears, it must be 21 bytes. Add one compact coral boundary indicating that an existing writable shared mapping can make F_SEAL_WRITE fail with EBUSY. Do not imply that seals are reversible, do not place F_SEAL_SEAL before the data seals, do not show reads being blocked, and do not depict the memfd as a normal pathname-backed disk file or as ordinary heap memory.

## 30 — runtime dynamic loading

Create a horizontal 3:2 premium editorial scientific-educational textbook raster illustration, not Mermaid and not SVG, on a subtle ivory paper texture. Use blue for the application, the glibc dynamic linker, handle or pointer representations, and the process-address-space libm mapping; amber only for an optional small kernel VMA, file, or page-cache support layer; green for the validated function result and orderly cleanup; coral for loader errors and ABI portability boundaries. Use dark navy outlines and typography, clean 2 px visual strokes, restrained shadows, no watermark, no logo, no code block, and no dense paragraph text inside the image.

Depict the actual stateful loader protocol as a numbered left-to-right flow with completely separated branches. Keep `dlopen`, `dlerror`, `dlsym`, and `dlclose` in the blue user-space loader/library layer rather than presenting them as system calls. 1) The blue application calls `dlopen` for libm.so.6 with concise RTLD_NOW and RTLD_LOCAL annotations. Its failure branch goes directly to `dlerror`, reports the loader message, and stops without a handle or any close attempt; never show a mapped object or handle on this branch. Its success branch alone lets the blue user-space dynamic linker map the shared object into the process address space and return a blue handle. 2) With that handle, one `dlerror` action clears stale state. 3) `dlsym` looks up cos. 4) Immediately call `dlerror` again. If this second check returns an error, draw a coral route that cannot touch or enter the ABI bridge: report the message, then perform handle-aware cleanup with `dlclose`, and stop. If there is no loader error, cross a clearly marked target-ABI bridge whose compile-time prerequisite is explicitly written as "object-pointer and function-pointer representations have equal size"; copy symbolic representation bits with `memcpy` and do not invent a numeric pointer address or show a direct cast. A resulting null function pointer is rejected before invocation, but a null `dlsym` result by itself was not the loader-error test. 5) Call `cos` with 0.0 and require the green value 1.0. 6) Invoke `dlclose`; a close failure leads to `dlerror` and an error report.

Render exactly these six distinct API labels, with precise spelling: `dlopen`, `dlerror`, `dlsym`, `memcpy`, `cos`, `dlclose`. Repeated `dlerror` and cleanup `dlclose` labels are allowed, but do not render any other API or function names. Do not treat a null symbol pointer alone as the definitive loader-error test, let any error path enter the ABI bridge, invent a concrete pointer value, portray libm.so.6 as a portable SONAME outside Linux/glibc, depict compile-time or static linking, claim ISO C guarantees this pointer conversion, or imply that successful `dlclose` necessarily unloads the code immediately.
